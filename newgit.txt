git bash in a new folder
git init
git config --global user.name "name"
git config --global user.email email
git help
 
touch filename.ext
git status 
git add

git commit -m "(changes made)"
-m added for adding a message
without -m a text editor will ask for a message

esc , cmd line :wq!
edit file
git diff filename.ext //changes

git log for changelog

git diff doesnt work after a new commit
git diff record changes
git remote set-url
git remote remove origin
git remote -v
git remote set-url alink

changes to the repository

git push origin master/(branch)
git checkout -b NewBranch
git checkout NewBranch
git checkout previouslogHASH // rollback
git branch -v
git merge NewBranch

ls

